target-version = "py314"
line-length = 100
extend-exclude = ["docs", "frontend"]

[lint]
select = [
  "E", "W", "F",       # pycodestyle, pyflakes
  "I",                 # isort
  "B",                 # bugbear
  "UP",                # pyupgrade
  "SIM",               # simplify
  "RUF",               # ruff-specific
  "ERA",               # commented-out code
  "T20",               # print in code
  "PT",                # pytest style
  "ASYNC",             # blocking calls in async
  "S",                 # bandit
  "N",                 # naming
  "C4",                # comprehensions
  "RET", "PIE", "TID",
]
ignore = [
  "E501",              # line length is the formatter's job
  "S101",              # assert in tests
  "RUF001", "RUF002", "RUF003",  # Cyrillic in strings and comments
]

[lint.per-file-ignores]
"tests/**" = ["S", "PT011", "TID251"]
"scripts/**" = ["T20", "S603", "S607"]
"packages/holtering/holtering/settings.py" = ["TID251"]
"packages/scp-holter/scp_holter/cli.py" = ["T20"]

[lint.flake8-tidy-imports.banned-api]
"os.environ".msg = "only holtering.settings reads the environment; everything else gets Settings from DI"
"os.getenv".msg = "only holtering.settings reads the environment; everything else gets Settings from DI"

[lint.isort]
known-first-party = ["scp_holter", "holtering", "tests"]

[lint.pydocstyle]
convention = "google"

[format]
quote-style = "double"
docstring-code-format = true
